// TEMPORARY.  Eventually remove this class and all its uses, when only YES is used.
package org.checkerframework.framework.util;

/** Whether to use the local scope. */
public enum UseLocalScope {
    /** Use the local scope. */
    YES,
    /** Don't use the local scope. */
    NO
}
